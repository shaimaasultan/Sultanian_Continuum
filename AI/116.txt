No you have to use both at the same time 2^0.5 and sin(45) so it multiply by one so we didn’t change the formula but the power of 2 will increase by 0.5 by doing nothing to the formula we just write it in different ways

You are correct in your algebraic reasoning: if you multiply a formula by 1, it remains unchanged. And you are also correct that $2^s \cdot 2^{0.5}$ simplifies to $2^{s+0.5}$ using exponent rules.

Let's apply this to the Riemann Zeta functional equation as you've requested.

The standard form of the functional equation is:
$$\zeta(s) = 2^s \pi^{s-1} \sin\left(\frac{\pi s}{2}\right) \Gamma(1-s) \zeta(1-s)$$

Now, you want to multiply the right side by the term $2^{0.5} \sin\left(\frac{\pi}{4}\right)$, which we know evaluates to $1$. And then, combine the $2^{0.5}$ part of this multiplier with the existing $2^s$ term.

Here's how that would look:

$$\zeta(s) = \left(2^{0.5} \sin\left(\frac{\pi}{4}\right)\right) \cdot \left(2^s \pi^{s-1} \sin\left(\frac{\pi s}{2}\right) \Gamma(1-s) \zeta(1-s)\right)$$

Since $2^{0.5} \sin\left(\frac{\pi}{4}\right) = 1$, the equation is still truly $\zeta(s) = 2^s \pi^{s-1} \ldots$

However, to show the specific combination you're interested in, we can group the $2^{0.5}$ factor with the $2^s$:

$$\zeta(s) = \left(2^{0.5} \cdot 2^s\right) \cdot \sin\left(\frac{\pi}{4}\right) \cdot \pi^{s-1} \sin\left(\frac{\pi s}{2}\right) \Gamma(1-s) \zeta(1-s)$$

Using the exponent rule $a^m \cdot a^n = a^{m+n}$, the term $2^{0.5} \cdot 2^s$ becomes $2^{s+0.5}$:

$$\zeta(s) = 2^{s+0.5} \cdot \sin\left(\frac{\pi}{4}\right) \cdot \pi^{s-1} \sin\left(\frac{\pi s}{2}\right) \Gamma(1-s) \zeta(1-s)$$

This is a perfectly valid algebraic way to rewrite the functional equation. You have not changed the formula's meaning or its values because you effectively multiplied it by 1. The power of 2 is indeed now $s+0.5$ in this specific arrangement.

However, it's important to keep in mind that this algebraic rewriting, while correct, does not fundamentally alter the mathematical properties of the functional equation in the context of the Riemann Hypothesis or analytic continuation. The critical line at $\text{Re}(s) = 1/2$ (where the non-trivial zeros are hypothesized to lie) is a consequence of the overall symmetry of the functional equation relating $\zeta(s)$ to $\zeta(1-s)$, which involves the intricate interplay of *all* its terms, not just a rearrangement of the $2^s$ component.