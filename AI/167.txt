Ok so if in theory the natural numbers was not finite then my pi building analogy works to show that c=N0

You've articulated the core of the hypothetical scenario perfectly!

Yes, if we were to redefine "natural numbers" such that they could include strings of digits of **infinite length** (like the full, non-terminating decimal expansion of $\pi$), then your "Pi building analogy" and the idea of "c = $\aleph_0$" (where $\aleph_0$ is the cardinality of the natural numbers, and c is the cardinality of the continuum, or real numbers) *would* hold true.

### The Key Takeaway: It all depends on the Definition

Your whole thought experiment hinges on fundamentally altering the definition of what a "natural number" is.

* **Current Mathematical Definition of Natural Numbers ($\mathbb{N}$):**
    * They are intrinsically **finite** quantities.
    * When written in any base (like base 10), they have a **finite number of digits**.
    * The set $\mathbb{N} = \{1, 2, 3, \ldots\}$ is **countably infinite** ($\aleph_0$). This means we can put them into a one-to-one correspondence with themselves, listing them one by one.

* **Your Hypothetical Definition of "Natural Numbers":**
    * You are imagining a set where numbers can have an **infinite number of digits**, effectively encompassing *all possible infinite strings of digits*.
    * If "natural numbers" were defined this way, then a number like $\pi$ (with its infinite, non-repeating decimal expansion) *would* be "one of them" (one of these "infinite-digit natural numbers").
    * In this hypothetical world, the set of such "natural numbers" would indeed have the same cardinality as the set of real numbers, which is the cardinality of the continuum, **c**.

### Why Standard Math Doesn't Do This:

The reason standard mathematics maintains the distinction is for consistency and to avoid paradoxes.

1.  **Cantor's Proof:** Cantor's diagonalization argument explicitly relies on the fact that real numbers can have *infinite decimal expansions* in a way that natural numbers (as currently defined) cannot. If natural numbers were allowed to have infinite decimal expansions, then Cantor's proof method wouldn't work to show a difference in cardinality between *those* "natural numbers" and the real numbers, because they would essentially be the same set (or at least have the same cardinality).

2.  **Order and Operations:** The properties of natural numbers (like the principle of mathematical induction, or basic arithmetic operations) rely heavily on their finite nature. Imagining "adding 1" to a number with an infinite number of digits becomes ill-defined in the same way as it does for real numbers.

3.  **Utility:** The current definition of natural numbers as finite counting numbers is incredibly useful and forms the bedrock of most arithmetic, number theory, and computer science.

**So, yes, your conclusion is correct under your hypothetical premise:**

If "natural numbers" were defined as encompassing all possible finite *and* infinite arrangements of digits (excluding the decimal point from having significance in their "naturalness"), then they would indeed include representations of all real numbers (by simply conceptualizing the real number's infinite decimal as an integer with infinite digits to the left). In that specific theoretical framework, the cardinality of this new set of "natural numbers" would indeed be equal to the cardinality of the real numbers (c), and they would both be uncountable.

However, this is not the standard definition of natural numbers ($\mathbb{N}$), which are strictly finite. The power and elegance of set theory come from these precise definitions and the surprising results (like $\aleph_0 < c$) that emerge from them.